// ksgpu::scratch(): invoked from the command line as 'ksgpu scratch'.
// A place for one-off hacks that won't be committed to git.

#include <complex>
#include <iostream>
#include "../include/ksgpu.hpp"
#include "../include/ksgpu/command_line_interface.hpp"

using namespace std;


// Called by 'python -m ksgpu scratch'. Intended for quick throwaway tests.
void ksgpu::scratch()
{
}
